@model Meals.Models.ShoppingListRecipe

@{
    ViewData["Title"] = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>ShoppingListRecipe</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>@Html.DisplayNameFor(model => model.ShoppingList)</dt>
        <dd>@Html.DisplayFor(model => model.ShoppingList.Name)</dd>
        <dt>@Html.DisplayNameFor(model => model.Recipe)</dt>
        <dd>@Html.DisplayFor(model => model.Recipe.Name)</dd>
        <dt>@Html.DisplayNameFor(model => model.Quantity)</dt>
        <dd>@Html.DisplayFor(model => model.Quantity)</dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="ShoppingListId" />
        <input type="hidden" asp-for="RecipeId" />
        <input type="submit" value="Delete" class="btn btn-default" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
